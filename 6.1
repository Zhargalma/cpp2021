#include <iostream>
#include <cstring>
using namespace std ;

int main()
{
    char str[] = "политех";
    char *start, *end;
    int a;
    char t;
    cout << "Исходная:" << str << "\n";
    a=strlen(str);
    start = str;
    end = &str[a-1];
    while(start < end) {
        t = *start;
        *start = *end;
        *end = t;
        start++;
        end--;
    }
        cout << "Перевернутая:" << str << "\n";
        return 0;
}
